#version 330 core

layout (location = 0) in vec2 aPos;

uniform vec4 aArg; // <x,y,w,h>

out VS_OUT
{
    vec4 arg;
}vs_out;



void main()
{
    vs_out.arg = aArg;
    gl_Position = vec4(0, 0, -1.0, 1.0);
}
#version 330 core

layout (location = 0) in vec2 aPos;

uniform vec4 aArg; // <x,y,w,h>

out VS_OUT
{
    vec4 arg;
}vs_out;



void main()
{
    vs_out.arg = aArg;
    gl_Position = vec4(0, 0, -1.0, 1.0);
}
#version 330 core

layout (location = 0) in vec2 aPos;

uniform vec4 aArg; // <x,y,w,h>

out VS_OUT
{
    vec4 arg;
}vs_out;



void main()
{
    vs_out.arg = aArg;
    gl_Position = vec4(0, 0, -1.0, 1.0);
}
#version 330 core

layout (location = 0) in vec2 aPos;

uniform vec4 aArg; // <x,y,w,h>

out VS_OUT
{
    vec4 arg;
}vs_out;



void main()
{
    vs_out.arg = aArg;
    gl_Position = vec4(0, 0, -1.0, 1.0);
}
#version 330 core

layout (location = 0) in vec2 aPos;

uniform vec4 aArg; // <x,y,w,h>

out VS_OUT
{
    vec4 arg;
}vs_out;



void main()
{
    vs_out.arg = aArg;
    gl_Position = vec4(0, 0, -1.0, 1.0);
}
#version 330 core

layout (location = 0) in vec2 aPos;

uniform vec4 aArg; // <x,y,w,h>

out VS_OUT
{
    vec4 arg;
}vs_out;



void main()
{
    vs_out.arg = aArg;
    gl_Position = vec4(0, 0, -1.0, 1.0);
}
#version 330 core

layout (location = 0) in vec2 aPos;

uniform vec4 aArg; // <x,y,w,h>

out VS_OUT
{
    vec4 arg;
}vs_out;



void main()
{
    vs_out.arg = aArg;
    gl_Position = vec4(0, 0, -1.0, 1.0);
}
#version 330 core

layout (location = 0) in vec2 aPos;

uniform vec4 aArg; // <x,y,w,h>

out VS_OUT
{
    vec4 arg;
}vs_out;



void main()
{
    vs_out.arg = aArg;
    gl_Position = vec4(0, 0, -1.0, 1.0);
}
#version 330 core

layout (location = 0) in vec2 aPos;

uniform vec4 aArg; // <x,y,w,h>

out VS_OUT
{
    vec4 arg;
}vs_out;



void main()
{
    vs_out.arg = aArg;
    gl_Position = vec4(0, 0, -1.0, 1.0);
}
#version 330 core

layout (location = 0) in vec2 aPos;

uniform vec4 aArg; // <x,y,w,h>

out VS_OUT
{
    vec4 arg;
}vs_out;



void main()
{
    vs_out.arg = aArg;
    gl_Position = vec4(0, 0, -1.0, 1.0);
}
#version 330 core

layout (location = 0) in vec2 aPos;

uniform vec4 aArg; // <x,y,w,h>

out VS_OUT
{
    vec4 arg;
}vs_out;



void main()
{
    vs_out.arg = aArg;
    gl_Position = vec4(0, 0, -1.0, 1.0);
}
#version 330 core

layout (location = 0) in vec2 aPos;

uniform vec4 aArg; // <x,y,w,h>

out VS_OUT
{
    vec4 arg;
}vs_out;



void main()
{
    vs_out.arg = aArg;
    gl_Position = vec4(0, 0, -1.0, 1.0);
}
#version 330 core

layout (location = 0) in vec2 aPos;

uniform vec4 aArg; // <x,y,w,h>

out VS_OUT
{
    vec4 arg;
}vs_out;



void main()
{
    vs_out.arg = aArg;
    gl_Position = vec4(0, 0, -1.0, 1.0);
}
#version 330 core

layout (location = 0) in vec2 aPos;

uniform vec4 aArg; // <x,y,w,h>

out VS_OUT
{
    vec4 arg;
}vs_out;



void main()
{
    vs_out.arg = aArg;
    gl_Position = vec4(0, 0, -1.0, 1.0);
}
#version 330 core

layout (location = 0) in vec2 aPos;

uniform vec4 aArg; // <x,y,w,h>

out VS_OUT
{
    vec4 arg;
}vs_out;



void main()
{
    vs_out.arg = aArg;
    gl_Position = vec4(0, 0, -1.0, 1.0);
}
#version 330 core

layout (location = 0) in vec2 aPos;

uniform vec4 aArg; // <x,y,w,h>

out VS_OUT
{
    vec4 arg;
}vs_out;



void main()
{
    vs_out.arg = aArg;
    gl_Position = vec4(0, 0, -1.0, 1.0);
}
#version 330 core

layout (location = 0) in vec2 aPos;

uniform vec4 aArg; // <x,y,w,h>

out VS_OUT
{
    vec4 arg;
}vs_out;



void main()
{
    vs_out.arg = aArg;
    gl_Position = vec4(0, 0, -1.0, 1.0);
}
#version 330 core

layout (location = 0) in vec2 aPos;

uniform vec4 aArg; // <x,y,w,h>

out VS_OUT
{
    vec4 arg;
}vs_out;



void main()
{
    vs_out.arg = aArg;
    gl_Position = vec4(0, 0, -1.0, 1.0);
}
#version 330 core

layout (location = 0) in vec2 aPos;

uniform vec4 aArg; // <x,y,w,h>

out VS_OUT
{
    vec4 arg;
}vs_out;



void main()
{
    vs_out.arg = aArg;
    gl_Position = vec4(0, 0, -1.0, 1.0);
}
#version 330 core

layout (location = 0) in vec2 aPos;

uniform vec4 aArg; // <x,y,w,h>

out VS_OUT
{
    vec4 arg;
}vs_out;



void main()
{
    vs_out.arg = aArg;
    gl_Position = vec4(0, 0, -1.0, 1.0);
}
#version 330 core

layout (location = 0) in vec2 aPos;

uniform vec4 aArg; // <x,y,w,h>

out VS_OUT
{
    vec4 arg;
}vs_out;



void main()
{
    vs_out.arg = aArg;
    gl_Position = vec4(0, 0, -1.0, 1.0);
}
#version 330 core

layout (location = 0) in vec2 aPos;

uniform vec4 aArg; // <x,y,w,h>

out VS_OUT
{
    vec4 arg;
}vs_out;



void main()
{
    vs_out.arg = aArg;
    gl_Position = vec4(0, 0, -1.0, 1.0);
}
